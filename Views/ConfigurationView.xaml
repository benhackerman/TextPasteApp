<Window x:Class="TextPasteApp.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration" Height="250" Width="400">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Shortcut Key:"/>
            <TextBox Text="{Binding Hotkey}"/>

            <CheckBox Content="Dark Mode" IsChecked="{Binding DarkModeEnabled}" Margin="0,10,0,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>